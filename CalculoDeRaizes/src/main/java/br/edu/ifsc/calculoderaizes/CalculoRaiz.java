/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package br.edu.ifsc.calculoderaizes;

/**
 *
 * @author Vitor
 */
public class CalculoRaiz {
    public static void main(String[] args) {
        //Bisseccao.calcularRaiz(1, 2, 0.01);
        //PosicaoFalsa.calcularRaiz(1, 2, 0.01);
        //Cordas.calcularRaiz(2.5, 3.5, Math.pow(10, -10));
        //PontoFixo.calcularRaiz(1.5, 0.01);
        //Newton.calcularRaiz(2, 0.001);
        //Newton.mostrarSinalDaMultiplicacaoDaFuncaoPelaSegundaDerivada(1);
        //Secante.calcularRaiz(0.6, 0.4, 0.001);
        
        //4d)
        //PontoFixo.calcularRaiz(1, Math.pow(10, -4));
        
        //4e)
        //Newton.mostrarDerivada(1);
        //Newton.mostrarDerivada(1.5);
        //Newton.mostrarSinalDaMultiplicacaoDaFuncaoPelaSegundaDerivada(1);
        //Newton.mostrarSinalDaMultiplicacaoDaFuncaoPelaSegundaDerivada(1.5);
        //Newton.calcularRaiz(1.5, Math.pow(10, -4));
        
        //4f)
        //Secante.mostrarFuncao(1);
        //Secante.calcularRaiz(1.5, 1, Math.pow(10, -4));
        
        //2A
        //a)
        //Bisseccao.calcularRaiz(1, 1.5, Math.pow(10, -3));
        //b)
        //PosicaoFalsa.calcularRaiz(1, 1.5, Math.pow(10, -4));
        //c)
        //Cordas.calcularRaiz(1.5, 1, Math.pow(10, -4));
        //e)
        //Newton.calcularRaiz(1.5, Math.pow(10, -4));
        //f)
        //Secante.mostrarFuncao(1);
        //Secante.calcularRaiz(1.5, 1, Math.pow(10, -4));
        
        //2B
        //a)
        //Bisseccao.calcularRaiz(2, 2.5, Math.pow(10, -3));
        //b)
        //PosicaoFalsa.calcularRaiz(2, 2.5, Math.pow(10, -4));
        //c)
        //Cordas.calcularRaiz(2.5, 2, Math.pow(10, -4));
        //e)
        //Newton.calcularRaiz(2.5, Math.pow(10, -4));
        //f)
        //Secante.calcularRaiz(2.5, 2, Math.pow(10, -4));
        
        //2C
        //a)
        //Bisseccao.calcularRaiz(1, 1.5, Math.pow(10, -3));
        //b)
        //PosicaoFalsa.calcularRaiz(1, 1.5, Math.pow(10, -4));
        //c)
        //Cordas.calcularRaiz(1.5, 1, Math.pow(10, -4));
        //e)
        //Newton.calcularRaiz(1.5, Math.pow(10, -4));
        //f)
        //Secante.calcularRaiz(1.5, 1, Math.pow(10, -4));
        
        //3
       
        //Newton.calcularRaiz(-1, Math.pow(10, -4));
       // Newton.calcularRaiz(5, Math.pow(10, -4));
        //Newton.mostrarFuncao(1);
        
        //6
        //PontoFixo.calcularRaiz(1, Math.pow(10, -2));
        
        //7
        //PontoFixo.calcularRaiz(1.0, 0.01);
        
        //8
        //PontoFixo.calcularRaiz(0, 0.001);
        
        //9
        //Newton.calcularRaiz(1.9, Math.pow(10, -3));
        
        //10
        Newton.calcularRaiz(1.5, Math.pow(10, -3));
        
    }
}
